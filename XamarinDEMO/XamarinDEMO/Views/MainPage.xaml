<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinDEMO.Views.MainPage">
    
    
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>

                <!-- Collection View -->
                <CollectionView ItemsSource="{Binding items}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Carousel View -->
                                <CarouselView Grid.Row="0" Margin="5" ItemsSource="{Binding items}" IndicatorView="indicatorView" HeightRequest="120">
                                <CarouselView.ItemTemplate>
                                    <DataTemplate>
                                        
                                        <StackLayout>
                                            <Frame HeightRequest="120">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    
                                                        <Image Grid.Row="0" Grid.RowSpan="3" Source="{Binding image}" Aspect="Fill" />
                                                    
                                                        <Label Grid.Row="0" Margin="5,0,0,0" Text="{Binding title}" />
                                                        <Label Grid.Row="2" Margin="5,0,0,0" Text="{Binding description}" />
                                                </Grid>
                                            </Frame>
                                        </StackLayout>
                                        
                                    </DataTemplate>
                                </CarouselView.ItemTemplate>
                            </CarouselView>

                                <!-- Indicator View -->
                                <IndicatorView Grid.Row="1" Margin="2" x:Name="indicatorView" Count="{Binding items.Count}" MaximumVisible="6" SelectedIndicatorColor="Black" IndicatorColor="Gray" IndicatorSize="3" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>