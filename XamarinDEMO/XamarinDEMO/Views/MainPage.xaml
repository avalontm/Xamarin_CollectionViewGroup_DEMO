<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="XamarinDEMO.Views.MainPage">


    <ContentPage.Content>
        <ScrollView>
            <StackLayout>

                <!-- Collection View -->
                <CollectionView ItemsSource="{Binding items}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Margin="5" Text="{Binding header}" TextColor="Black" FontAttributes="Bold" FontSize="18"/>
                                
                                <!-- Carousel View -->
                                <CarouselView Grid.Row="1" Margin="5" ItemsSource="{Binding items}" IndicatorView="indicatorView" HeightRequest="160">
                                    <CarouselView.ItemTemplate>
                                        <DataTemplate>

                                            <StackLayout>
                                                <yummy:PancakeView HeightRequest="160" Margin="5,0,5,0" BackgroundColor="White" CornerRadius="10">
                                                    <Grid>

                                                        <Image Grid.Row="0" Source="{Binding image}" Aspect="Fill" />

                                                        <yummy:PancakeView Grid.Row="0" Padding="5" BackgroundColor="Black" Opacity="0.5">
                                                            <Label Margin="5,0,0,0" Text="{Binding title}" TextColor="White" FontAttributes="Bold" FontSize="24" VerticalTextAlignment="Center" HorizontalTextAlignment="Center"/>
                                                        </yummy:PancakeView>

                                                    </Grid>
                                                </yummy:PancakeView>
                                            </StackLayout>

                                        </DataTemplate>
                                    </CarouselView.ItemTemplate>
                                </CarouselView>

                                <!-- Indicator View -->
                                <IndicatorView Grid.Row="2" Margin="2" x:Name="indicatorView" Count="{Binding items.Count}" MaximumVisible="6" SelectedIndicatorColor="Black" IndicatorColor="Gray" IndicatorSize="5" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>